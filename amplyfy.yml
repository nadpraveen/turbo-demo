version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        build:
          commands:
            - yarn build:
        artifacts:
          files:
            - "**/*"
          baseDirectory: apps/web/build
        cache:
          paths:
            - node_modules/**/*
